tests:
  gpio.button.toggle:
    build_only: false
    harness: shell
    platform_allow:
      - nrf5340dk/nrf5340/cpuapp
    extra_configs:
      - CONFIG_GPIO_EMUL=y
    harness_config:
      shell_commands:
        - command: "test_btn"
          expected: "Test: Triggering emulated button press"
        # - command: "test_btn"
        #   expected: "Button pressed! LED is now ON"
        # - command: "test_btn"
        #   expected: "Button pressed! LED is now OFF"
        # - command: "test_btn"
        #   expected: "Button pressed! LED is now ON"
        # - command: "help"
        #   expected: "Please press the <Tab> button"
