name: Blinky

on:
#   push:
#     branches: [ main ]
#   pull_request:
#   schedule:
#     - cron: "0 0 * * *"

  workflow_dispatch:

jobs:
  build_for_hw_test:
    runs-on: ubuntu-latest

    container:
      image: zephyrprojectrtos/ci:v0.29.3

    env:
      ZEPHYR_VERSION: v4.2.2
      # If the toolchain baked into the ci image doesn't match this,
      # uncomment and point at an explicitly installed SDK instead:
      # ZEPHYR_SDK_INSTALL_DIR: /opt/toolchains/zephyr-sdk-0.17.0

    steps:
      - uses: actions/checkout@v4
        with:
          path: app

      - name: Cache Zephyr workspace
        uses: actions/cache@v4
        id: zephyr-cache
        with:
          path: zephyr-workspace
          key: ${{ runner.os }}-zephyr-${{ env.ZEPHYR_VERSION }}

      - name: Fetch vanilla Zephyr
        if: steps.zephyr-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p zephyr-workspace
          git clone --depth 1 --branch $ZEPHYR_VERSION \
            https://github.com/zephyrproject-rtos/zephyr.git zephyr-workspace/zephyr
          cd zephyr-workspace
          west init -l zephyr
          west update --narrow -o=--depth=1

      - name: Cache Ccache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ${{ runner.os }}-ccache-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-${{ github.ref_name }}-
            ${{ runner.os }}-ccache-

      - name: Build app against pinned Zephyr
        env:
          CCACHE_DIR: ~/.cache/ccache
        run: |
          cd zephyr-workspace
          west build -b nrf5340dk_nrf5340_cpuapp "${{ github.workspace }}/app" -d build -p -- -DZEPHYR_SCACHE=ccache

      - name: Create build tarball
        run: |
          cd zephyr-workspace/build/zephyr
          tar czf build.tar.gz zephyr.hex

      - name: Upload tarball
        uses: actions/upload-artifact@v4
        with:
          name: build.tar.gz
          path: zephyr-workspace/build/zephyr/build.tar.gz

# Run on device
  hw_flash_and_test:
    needs: build_for_hw_test
    runs-on: [self-hosted]

    defaults:
      run:
        shell: bash
        working-directory: blinky-test

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          path: blinky-test

      - name: Download build tarball
        uses: actions/download-artifact@v4
        with:
          name: build.tar.gz
          path: blinky-test/samples/blinky

      - name: Untar
        run: |
          cd samples/blinky
          tar xvf build.tar.gz

      - name: Flash and Verify Test Results
        env:
          CI_NRF5340DK_SNR: 001050073602
        run: |
          cd samples/blinky

          # Check serial number of board from GitHub Repo Secret
          echo $CI_NRF5340DK_SNR

          nrfjprog --recover -f NRF53 --snr $CI_NRF5340DK_SNR
          nrfjprog --eraseall -f NRF53 --snr $CI_NRF5340DK_SNR
          nrfjprog --program build/zephyr/zephyr.hex --verify -f NRF53 --snr $CI_NRF5340DK_SNR
          nrfjprog --pinresetenable -f NRF53 --snr $CI_NRF5340DK_SNR
          nrfjprog --pinreset -f NRF53 --snr $CI_NRF5340DK_SNR
          sleep 3